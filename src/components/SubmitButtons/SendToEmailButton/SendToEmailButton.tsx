const SendToEmailButton = () => (
  <input
    type="submit"
    className="button is-fullwidth is-light"
    value="Send to email ⚡️"
    data-destination="mail"
  />
);

export default SendToEmailButton;
